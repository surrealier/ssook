# Optional VLM backend dependencies.
# Only needed for the `transformers` (local Qwen-VL etc.) and `openai`
# (OpenAI-compatible HTTP) backends. The default `clip` backend stays
# dependency-free and works with just requirements-web.txt.
#
#   pip install -r requirements-vlm.txt
#
# For GPU, install the CUDA build of torch from the PyTorch index instead:
#   pip install torch --index-url https://download.pytorch.org/whl/cu124

# Local transformers backend (Qwen2.5-VL and other image-text-to-text models)
transformers>=4.50,<5
torch>=2.2
accelerate>=0.30
qwen-vl-utils>=0.0.8   # optional, only for Qwen-VL preprocessing helpers

# OpenAI-compatible HTTP backend (Ollama / vLLM / LM Studio)
httpx>=0.27
